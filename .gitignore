node_modules
# Anchored to repo root only (2026-09-11 fix) — a bare "dist" here was
# blanket-excluding node_source/spCoinPanels/.../spcoin-panels/dist/ too,
# the actual compiled JS the local `file:` dependency needs. That package
# is committed WITH its dist/ (matching spcoin-common's own precedent in
# the front-end repo — no .gitignore for it there either), so only this
# repo's own build output should be ignored. Renamed dist -> deploy
# (2026-09-11, same day, on request) — see vite.config.ts's own comment.
/deploy
.env
.env.local
.DS_Store
